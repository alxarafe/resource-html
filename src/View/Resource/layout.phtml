<?php
/**
 * Layout principal para ResourceController — HTML puro (sin Twig).
 *
 * Variables disponibles:
 *   $title        — Título de la página
 *   $bodyContent  — HTML del contenido principal (ya renderizado)
 *   $lang         — Código de idioma (ej: 'es')
 *   $csrfToken    — Token CSRF
 */

$lang = $lang ?? substr(defined('FS_LANG') ? FS_LANG : 'es', 0, 2);
$title = htmlspecialchars($title ?? 'Tahiche', ENT_QUOTES, 'UTF-8');
?><!DOCTYPE html>
<html lang="<?= $lang ?>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="robots" content="noindex">
    <title><?= $title ?></title>
    <link rel="shortcut icon" href="Dinamic/Assets/Images/favicon.ico">
    <link rel="stylesheet" href="node_modules/bootstrap/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="node_modules/@fortawesome/fontawesome-free/css/all.min.css">
    <link rel="stylesheet" href="Dinamic/Assets/CSS/custom.css">
    <style>
        .resource-header { background: linear-gradient(135deg, #1a237e 0%, #283593 100%); }
        .resource-card { border: none; border-radius: 0.75rem; box-shadow: 0 2px 12px rgba(0,0,0,.08); }
        .table thead th { border-bottom: 2px solid #dee2e6; font-weight: 600; font-size: 0.85rem; text-transform: uppercase; letter-spacing: 0.05em; color: #495057; }
        .table tbody tr:hover { background-color: rgba(26,35,126,.04); }
        .form-label { font-weight: 600; font-size: 0.875rem; color: #495057; }
        .btn-resource-primary { background: #1a237e; border-color: #1a237e; color: #fff; }
        .btn-resource-primary:hover { background: #283593; border-color: #283593; color: #fff; }
    </style>
</head>
<body class="bg-light">
    <?= $bodyContent ?? '' ?>

    <script src="node_modules/jquery/dist/jquery.min.js"></script>
    <script src="node_modules/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
